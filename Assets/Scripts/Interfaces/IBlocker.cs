public interface IBlocker
{
    /*
    This interface was created for blocking any object with this script in some point of the gameplay.
    Before to find the solution of teleport and Move, I tried to block any movement meanwhile player was keep looking a
    interact zone but any thing changed. The error was not here (!)
     */
     
    //public delegate void DelegateBlocker(IBlocker b);
    //public static DelegateBlocker OnBlocker; //to notice someother scripts
    //public abstract IBlocker NoticeBlocker(); // to get interface ref
    //public abstract bool Block { get; set; } //for make conditions.
}
